[build-system]
build-backend = "uv_build"
requires = [ "uv-build>=0.8.5" ]

[project]
name = "ecc"
version = "0.1.0-alpha.6"
readme = "README.md"
authors = [
  { name = "Emin", email = "me@emin.chat" },
  { name = "KoEkko", email = "225193046@qq.com" },
  { name = "Yell-walkalone", email = "12112088@qq.com" },
]
requires-python = ">=3.11"
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "ecc-dreamplace==0.1.0a5",
  "ecc-tools-bin==0.1.0a7",
  "fastapi>=0.109",
  "jsonrpcserver>=5.0.9",
  "klayout>=0.30.2",
  "matplotlib>=3.4",
  "numpy>=1.21",
  "pandas>=1.3",
  "pip>=25.0.1",
  "pydantic>=2.5",
  "pyjson5>=1.6",
  "pyyaml>=6",
  "rosettakit==0.2.0",
  "scipy>=1.7",
  "torch>=1.6.0",
  "tqdm>=4.67.1",
  "typer>=0.12",
  "uvicorn>=0.27",
]
scripts.ecc = "chipcompiler.cli.main:main"

[dependency-groups]
dev = [
  "auditwheel>=6",
  "black>=23",
  "mypy>=1",
  "prek>=0.2.20",
  "pyinstaller==6.17",
  "pyinstaller>=6",
  "pyright>=1.1.407",
  "pytest>=7",
  "pytest-cov>=4",
  "ruff>=0.14.10",
  "scikit-build-core>=0.12.2",
  "ty>=0.0.9",
  "uv-build>=0.10.9,<0.11.0",
  "z3-solver>=4.12",
]

[tool.uv]
constraint-dependencies = [
  "llvmlite>=0.40.0",
  "numba>=0.57.0",
]
build-backend.module-name = [ "chipcompiler" ]
build-backend.module-root = ""
build-backend.source-exclude = [
  "/scripts/**",
  "chipcompiler/thirdparty/ecc-tools/src/third_party/BST-DME/**"
]
build-backend.wheel-exclude = [
  "chipcompiler/thirdparty/**",
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.workspace]
members = [
  "chipcompiler/thirdparty/ecc-dreamplace",
  "chipcompiler/thirdparty/ecc-tools",
]

[tool.uv.sources]
ecc-dreamplace = { workspace = true }
ecc-tools-bin = { workspace = true }
torch = { index = "pytorch-cpu" }

[tool.ruff]
line-length = 100
lint.select = [
  "B",   # flake8-bugbear
  "E",   # pycodestyle
  "F",   # Pyflakes
  "I",   # isort
  "SIM", # flake8-simplify
  "UP",  # pyupgrade
]
extend-include = ["*.spec"]

[tool.pytest.ini_options]
testpaths = ["test"]
norecursedirs = [".venv"]
markers = [
  "integration: tests that run an ECC flow or large workspace orchestration",
  "pdk: tests that require a complete external PDK installation",
  "slow: tests that are expected to be expensive on a normal developer shell",
]
addopts = [
  "--deselect=test/integration/test_rtl2gds_flow.py::test_sg13g2_gcd",
]

[tool.ty]
environment.python-version = "3.11"
src.include = [ "chipcompiler/**/*.py", "tests/**/*.py" ]
